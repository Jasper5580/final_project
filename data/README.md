# Data folders

`00_raw` and `02_manual_validation/final` contain the immutable inputs needed for a full computational rebuild. `01_cleaned` and `03_analysis_ready` contain supplied snapshots for inspection and are overwritten by `run_all.R`. Manual validation decisions are frozen human judgements and cannot be recreated automatically.
