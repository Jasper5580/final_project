# Log folders

`reference_run` is the supplied final-run audit trail. Each new `run_all.R` execution creates a timestamped run folder with a master log, one log per script, a manifest and session information.
